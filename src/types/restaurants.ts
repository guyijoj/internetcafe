export type restaurantsInfo = {
  id: string;
  address: string;
  phone: string;
  city: string;
  workingHours: string;
  photo: string;
  link: string;
};
export type RestaurantsModalPayload = {
  fromCheckout: boolean;
};
